{
  "name": "launchradar",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter launchradar-backend dev",
    "dev:api": "pnpm --filter launchradar-backend dev:api",
    "dev:web": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter launchradar-backend test"
  }
}
