# Local dev: Vite proxies /api/* to the backend (see vite.config.ts).
# Omit to use the default /api base in development (see src/api-base.ts).
VITE_API_URL=/api

# Production: set to your API origin or same-host path, e.g. https://api.example.com or /api
